version: '3.4'

services:
  cssure:
      image: memilnie/cssure:v1.1-dev
      container_name: cssure_dotnet

      restart: unless-stopped

  python:
      image: memilnie/csi_calculator:v1.1-dev
      network_mode: bridge
      container_name: cssure_python

      restart: unless-stopped
